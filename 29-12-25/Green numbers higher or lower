def guessNumber(n):
    l, r = 1, n
    while l <= r:
        m = (l + r) // 2
        if guess(m) == 0:
            return m
        elif guess(m) < 0:
            r = m - 1
        else:
            l = m + 1
